<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simon.study">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--<activity android:name=".MainActivity">-->
        <!--<activity android:name=".scroll.ScrollAct">-->
        <!--<activity android:name=".webview.WebViewAct">-->
        <!--<activity android:name=".view.wordmagnifier.MainActivity">-->
        <!--<activity android:name=".view.word_magnifier.MainActivity">-->
        <activity android:name=".layoutparams.LayoutParamsAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--该Activity在statck中不留历史痕迹。默认的值是false-->
        <!--<activity-->
        <!--android:name=".scroll.ScrollAct"-->
        <!--android:launchMode="singleTop"-->
        <!--android:noHistory="true"-->
        <!--android:screenOrientation="portrait" />-->

    </application>

</manifest>